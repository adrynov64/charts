apiVersion: v1
kind: Service
metadata:
  name: {{ include "tp4.fullname" . }}
  labels:
    {{- include "tp4.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "tp4.selectorLabels" . | nindent 4 }}